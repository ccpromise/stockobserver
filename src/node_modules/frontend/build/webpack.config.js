
var path = require('path');

module.exports = {
    // absolute path of entry file index.js
    entry: path.join(__dirname, '../src/index.js'),
    // output config
    output: {
        path: path.join(__dirname, '../src/dist'),
        filename: 'main.js',
        publicPath: '../src/dist/'
    },
    resolve: {
        extensions: ['.js', '.vue'],
        alias: {
         vue: 'vue/dist/vue.min.js'
       }
    },
    module: {
        loaders: [
            {
                test: /\.vue$/,
                loader: 'vue-loader'
            },
            {
                test: /\.js$/,
                loader: 'babel-loader?presets=es2015',
                exclude: /node_modules/
            }
        ]
    },
}
