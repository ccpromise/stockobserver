
const validate = require('validate');

exports.symbol2secID = function (symbol) {
    if (!(validate.isStr(symbol) && symbol.length === 6 && (symbol[0] === '0' || symbol[0] === '3' || symbol[0] === '6')))
        return symbol;
    return symbol + (symbol[0] === '6' ? '.xshg' : '.xshe');
}
